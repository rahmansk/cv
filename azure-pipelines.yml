# azure-pipelines.yml

trigger:
  - main   # or your branch name

pool:
  name: EC2-Ubuntu-Agent   # Must match your self-hosted pool exactly

steps:
  - script: echo "Running on EC2 self-hosted agent"
    displayName: "Test self-hosted agent"
